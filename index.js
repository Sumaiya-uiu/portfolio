var typed = new Typed('#element', {
    strings: ['<i>A Recent CS Graduate</i>', '<i>Frontend Developer</i>', '<i>Problem solver</i>',],
    typeSpeed: 50,
    loop: true,
    backDelay: 3000,
    backSpeed: 30,
});